# CONTINUITY

## [PLANS]
- 2026-03-21T08:08:03-04:00 [USER] Keep `AGENTS.md` concise, preserve key repo rules, reduce overlap, and reorder issue execution so issue review and planning happen before branch creation.
- 2026-03-21T08:46:08-04:00 [USER] Implement issue `#22` for real Mercado Pago initiation and verified webhook reconciliation after the planning pass.
- 2026-03-25T13:27:46-04:00 [USER] Request an implementation plan for `FR-025` and `FR-026` based on the current `docs/requirements.md` scope and existing Mercado Pago integration progress.
- 2026-03-25T13:27:46-04:00 [USER] Implement the backend-only `FR-025` and `FR-026` completion plan in the direction established by merged PR `#43`, with frontend explicitly deferred.
- 2026-03-30T09:20:48-04:00 [USER] Verify current project state, map current open issues that should be started, and recommend next steps against `docs/tech-architecture.md`.
- 2026-03-30T11:18:11-04:00 [USER] Audit the current security posture with the security best-practices skill and implement the resulting hardening plan.

## [DECISIONS]
- 2026-03-21T08:08:03-04:00 [CODE] `AGENTS.md` now uses a consolidated structure with a global operating baseline first and repo-specific rules second.
- 2026-03-21T08:08:03-04:00 [CODE] Issue workflow order is now: inspect issue, check overlap, prepare plan, then create branch/worktree/PR, then implement and verify.
- 2026-03-21T08:08:03-04:00 [CODE] Workspace change history should live in `.agent/CHANGELOG.md` instead of inline in `AGENTS.md`.
- 2026-03-21T08:08:03-04:00 [CODE] `AGENTS.md` now explicitly distinguishes `.agent/CONTINUITY.md` as current-state handoff context and `.agent/CHANGELOG.md` as durable project history.
- 2026-03-21T08:46:08-04:00 [CODE] Issue `#22` implementation uses Checkout Pro preference creation, a provider-specific Mercado Pago webhook route, and approved-first reconciliation while still persisting non-approved provider statuses.
- 2026-03-21T08:46:08-04:00 [CODE] `packages/integrations` is now being formalized as `@packages/integrations` with the official Mercado Pago SDK instead of continuing the old alias-only pattern.
- 2026-03-25T13:27:46-04:00 [CODE] `FR-025` and `FR-026` follow PR `#43`'s backend-only Checkout Pro direction; remaining work should harden the existing adapter and cover the supported methods rather than introduce frontend flows or broaden webhook state transitions.
- 2026-03-30T11:18:11-04:00 [CODE] Sensitive order/game credentials are now protected with application-layer AES-256-GCM encryption-at-rest using a versioned envelope format in existing Prisma string columns rather than a schema change.
- 2026-03-30T11:18:11-04:00 [CODE] Production env parsing must now reject development placeholder values for JWT secrets, the internal API key, and the order-credentials encryption key, while development and test keep convenience defaults.
- 2026-03-30T11:18:11-04:00 [CODE] Auth abuse controls are now explicit at the controller boundary: login and refresh use a dedicated throttler guard with independent settings, while logout remains unthrottled.
- 2026-03-30T12:09:00-04:00 [CODE] Order-credentials encryption-key validation now fails fast in shared config parsing and in the cipher service constructor via a single reusable validator, instead of waiting for first encrypt/decrypt use.
- 2026-03-30T12:09:00-04:00 [CODE] Auth and users throttling now share a common configurable Nest guard and a global HTTP throttling module; feature modules only provide route-specific throttle mappings.
- 2026-03-30T12:09:00-04:00 [CODE] API test setup now centralizes `AppSettingsService` overrides through `apps/api/test/test-app-settings.ts` to reduce fixture drift when settings change.

## [PROGRESS]
- 2026-03-30T11:22:02-04:00 [TOOL] Created GitHub issue `#46` from the repository issue template for script convention cleanup, using labels `refactor`, `tech-debt`, and `infra`, without changing application source files.
- 2026-03-21T08:08:03-04:00 [TOOL] Rewrote `AGENTS.md` to merge the requested baseline with existing repo-specific rules and added this continuity file scaffold.
- 2026-03-21T08:08:03-04:00 [TOOL] Moved the existing inline `AGENTS.md` changelog into `.agent/CHANGELOG.md` and updated maintenance guidance to point there.
- 2026-03-21T08:08:03-04:00 [TOOL] Added an explicit `CONTINUITY.md` versus `CHANGELOG.md` boundary section to `AGENTS.md`.
- 2026-03-21T08:23:58-04:00 [TOOL] Verified merged PR `#42` (`40-prepare-payments-provider-contract`) against GitHub issue `#40`, the merged diff on `main`, Prisma schema constraints, and targeted payments tests.
- 2026-03-21T08:46:08-04:00 [TOOL] Created and pushed branch `22-mercadopago-wrapper-webhooks`; draft PR creation was attempted first but GitHub rejected it until the branch has at least one commit ahead of `main`.
- 2026-03-21T08:46:08-04:00 [TOOL] Implemented the non-DB portion of issue `#22`, ran `pnpm biome:fix:all`, ran `pnpm --filter api exec tsc --noEmit -p tsconfig.json`, reran targeted unit/integration tests, and reran `pnpm --filter api test:e2e -- payments.e2e-spec.ts`.
- 2026-03-21T12:50Z [TOOL] Reproduced the DB-backed payments failure, regenerated Prisma with `pnpm -w db:generate`, and reran `pnpm --filter api test:integration:db -- payments.db.integration.spec.ts` to green.
- 2026-03-21T09:16:13-04:00 [TOOL] Installed the external `nestjs-doctor` skill from `RoloBits/nestjs-doctor` path `packages/nestjs-doctor/skill` into `~/.codex/skills/nestjs-doctor`.
- 2026-03-21T13:03Z [TOOL] Hardened the Mercado Pago webhook path against replay-by-body-id and unsupported topics, then reran webhook-focused unit/integration/e2e coverage plus `pnpm biome:fix:all`, API typecheck, and the DB-backed payments lane.
- 2026-03-21T13:07Z [TOOL] Closed the remaining alias-replay gap by removing topic from the processed webhook key, added alias-replay regressions, and reran the webhook-focused, e2e, formatting, typecheck, and DB-backed payments verification lanes.
- 2026-03-25T13:27:46-04:00 [TOOL] Added Mercado Pago adapter coverage for `credit_card`, `pix`, and `boleto` Checkout Pro preference exclusions plus a runtime guard against unsupported payment methods, then added API e2e coverage for `credit_card` and `boleto` payment creation.
- 2026-03-25T13:27:46-04:00 [TOOL] Reran `pnpm biome:fix:all`, `pnpm --filter api exec tsc --noEmit -p tsconfig.json`, `pnpm --filter api exec jest --runInBand test/integration/payments/mercadopago-sdk.integration.spec.ts`, `pnpm --filter api exec jest --runInBand test/integration/payments/payments.integration.spec.ts`, and `pnpm --filter api test:e2e -- payments.e2e-spec.ts`.
- 2026-03-30T09:20:48-04:00 [TOOL] Inspected `docs/tech-architecture.md`, `docs/requirements.md`, current module/package tree, and open GitHub issues to compare implemented slices against the documented architecture and roadmap.
- 2026-03-30T09:20:48-04:00 [TOOL] Verified the current repo still has a clean git worktree, passes `pnpm --filter api exec tsc --noEmit -p tsconfig.json`, and passes `pnpm exec biome check .`.
- 2026-03-30T10:40:58-04:00 [TOOL] Created branch `5-order-extras-deterministic-pricing` after `git fetch` and `git pull`, carrying the current `.agent/CONTINUITY.md` changes onto the new issue branch as requested.
- 2026-03-30T10:40:58-04:00 [TOOL] Implemented the initial `#5` slice with strict TDD on core behaviors: added deterministic extras pricing at quote time, carried priced extras through quote-to-order creation in memory and Prisma paths, rejected duplicate extras at the Zod boundary, regenerated Prisma Client, and reran targeted unit/integration/e2e verification plus Biome and API typecheck.
- 2026-03-30T10:43:19-04:00 [TOOL] After the operator confirmed the Prisma migration was already applied, reran DB-backed verification with `pnpm --filter api test:integration:db -- orders.db.integration.spec.ts` and `pnpm --filter api test:integration:db -- payments.db.integration.spec.ts`.
- 2026-03-30T10:47:39-04:00 [TOOL] Reran the DB-backed lanes sequentially instead of in parallel: `pnpm --filter api test:integration:db -- orders.db.integration.spec.ts` then `pnpm --filter api test:integration:db -- payments.db.integration.spec.ts`.
- 2026-03-30T10:51:18-04:00 [TOOL] Reran the broader API Jest suite with `pnpm --filter api test`, identified two remaining failures, fixed them, then reran the targeted failing specs, the full API Jest suite, `pnpm biome:fix:all`, and `pnpm --filter api exec tsc --noEmit -p tsconfig.json`.
- 2026-03-30T10:59:32-04:00 [TOOL] Implemented the remaining `#5` immutability slice with TDD: added a domain regression test for external extras mutation, fixed the order aggregate to clone extras defensively, added integration and DB-integration tests proving extras remain unchanged through later order lifecycle transitions, then reran the full API Jest suite, DB-backed orders/payments lanes, Biome, and API typecheck.
- 2026-03-30T11:18:11-04:00 [TOOL] Audited backend/frontend security surfaces against the security skill references and confirmed concrete gaps in raw order credential storage, production secret defaults, missing auth throttling, and missing app-level HTTP hardening.
- 2026-03-30T11:18:11-04:00 [TOOL] Implemented security hardening with TDD: added env-schema regression tests, auth e2e throttling/header coverage, and DB-backed encrypted-credentials coverage before changing the underlying config, auth guard, repository, and HTTP bootstrap code.
- 2026-03-30T11:18:11-04:00 [TOOL] Reran `pnpm --filter api test`, `pnpm --filter api test:e2e -- auth.e2e-spec.ts`, `pnpm --filter api test:integration:db -- orders.db.integration.spec.ts`, `pnpm --filter api test:integration:db -- payments.db.integration.spec.ts`, `pnpm biome:fix:all`, and `pnpm --filter api exec tsc --noEmit -p tsconfig.json` to green after the security changes.
- 2026-03-30T12:09:00-04:00 [TOOL] Implemented the post-review maintainability hardening: added shared order-credentials key validation in `packages/config`, replaced duplicate auth/users throttling logic with common HTTP infrastructure, centralized API test settings fixtures, and cleaned the HTTP bootstrap helper without changing its runtime behavior.
- 2026-03-30T12:09:00-04:00 [TOOL] Verified the maintainability refactor with `pnpm --filter api exec jest --runInBand src/common/settings/app-settings.service.spec.ts src/modules/orders/infrastructure/security/order-credentials-cipher.service.spec.ts test/integration/auth/auth.integration.spec.ts`, `pnpm --filter api test`, `pnpm --filter api test:e2e -- auth.e2e-spec.ts`, `pnpm biome:fix:all`, and `pnpm --filter api exec tsc --noEmit -p tsconfig.json`.
- 2026-03-30T12:09:00-04:00 [TOOL] Reran the DB-backed lanes sequentially as the final meaningful verification: `pnpm --filter api test:integration:db -- orders.db.integration.spec.ts` followed by `pnpm --filter api test:integration:db -- payments.db.integration.spec.ts`.

## [DISCOVERIES]
- 2026-03-21T08:08:03-04:00 [TOOL] `.agent/CONTINUITY.md` did not exist before this change.
- 2026-03-21T08:23:58-04:00 [TOOL] Issue `#40` is a narrowed prerequisite for `FR-025`, not the full Mercado Pago integration; its scope is local payment identity ownership, gateway field persistence, and duplicate-protection groundwork before issue `#22`.
- 2026-03-21T08:23:58-04:00 [TOOL] Prisma already enforces persistent uniqueness for `Payment.gatewayId` and the diff relies on app-level duplicate checks plus the existing schema-level constraint on `Payment.orderId`.
- 2026-03-21T08:46:08-04:00 [TOOL] DB-backed payments tests currently fail with `Unknown argument gatewayReferenceId` because the Prisma client/database still reflect the old `Payment` shape; the code/schema changes now require a human-run Prisma migration and regenerated client before DB verification can pass.
- 2026-03-21T12:50Z [TOOL] Applying the migration was not sufficient for DB verification; Prisma Client also had to be regenerated locally before the repository/test code recognized `gatewayReferenceId` and `gatewayStatusDetail`.
- 2026-03-21T13:03Z [CODE] Using Mercado Pago body `id` as the dedupe key was unsafe because the signature manifest does not cover that field; replay protection is now keyed from provider-bound `topic + notificationResourceId` instead.
- 2026-03-21T13:07Z [CODE] Including topic in the processed webhook key was still redundant and unsafe because Mercado Pago signature verification does not bind topic; the canonical replay key is now only the provider notification resource id.
- 2026-03-25T13:27:46-04:00 [TOOL] After PR `#43`, the backend already handled Mercado Pago Checkout Pro initiation and approved-first webhook reconciliation; the practical remaining `FR-025`/`FR-026` gap was explicit method hardening and coverage, not new payment flow wiring.
- 2026-03-30T09:20:48-04:00 [TOOL] The implemented backend architecture currently covers `auth`, `users`, `orders`, `payments`, `wallet`, and `system`, plus the `apps/workers` wallet release worker; `chat`, `tickets`, `ratings`, `admin`, and `services` modules are still absent from `apps/api`.
- 2026-03-30T09:20:48-04:00 [TOOL] `apps/web` remains scaffold-level with only `app/layout.tsx`, `app/page.tsx`, and `app/globals.css`, and `packages/ui` is still empty except for `.gitkeep`.
- 2026-03-30T09:20:48-04:00 [TOOL] The roadmap item for RBAC in `AGENTS.md` is stale: `@Roles()` and `RolesGuard` are already implemented and used in `orders` and `payments`.
- 2026-03-30T09:20:48-04:00 [TOOL] Open GitHub issues are `#41`, `#34`, `#33`, `#23`, and `#5`; issue `#23` still lists blockers `#22` and `#6`, but both are already closed.
- 2026-03-30T09:20:48-04:00 [TOOL] `docs/tech-architecture.md` calls for Swagger/OpenAPI, `nestjs-pino`, and OpenTelemetry, but no current source references those integrations yet.
- 2026-03-30T10:40:58-04:00 [CODE] The existing Prisma schema already had `OrderExtra` on `Order`, but quote-driven checkout still needed quote-side extras persistence to avoid losing selected extras between quote creation and order creation.
- 2026-03-30T10:40:58-04:00 [TOOL] The branch now changes Prisma schema and generated client shape for quote/order extras, but no Prisma migration has been generated yet because repo rules require the human operator to run `pnpm db:migrate:dev --name <migration_name>` manually after schema review.
- 2026-03-30T10:43:19-04:00 [TOOL] The new migration `20260330144233_add_order_quote_extras` applied successfully in the test database during DB verification, so the remaining failures are no longer migration-blocked.
- 2026-03-30T10:43:19-04:00 [TOOL] DB-backed failures cluster around the Prisma checkout path: `PrismaOrderCheckoutRepository.createDraftOrderFromOwnedQuote` now throws `OrderQuoteAlreadyUsedError` in non-concurrent paths, and downstream payment tests then fail with missing order/payment records or FK cleanup breakage.
- 2026-03-30T10:43:19-04:00 [TOOL] Additional DB-backed regression appeared in `PrismaOrderQuoteRepository.create` when coupon-linked quotes are created, surfacing `order_quotes_couponId_fkey` failures in order DB tests.
- 2026-03-30T10:47:39-04:00 [TOOL] Supersedes the previous DB-failure diagnosis: the earlier red DB results were caused by running `orders.db.integration.spec.ts` and `payments.db.integration.spec.ts` concurrently against the same `elonew_test` database, not by a persisted checkout regression in the current code.
- 2026-03-30T10:51:18-04:00 [TOOL] One broader-suite failure was a stale auth test fixture in `refresh-session.use-case.spec.ts`: the mock refresh-session expiry date had drifted into the past relative to the current date, so the test was failing due to expired fixture data rather than a refresh-session code regression.
- 2026-03-30T10:51:18-04:00 [CODE] The other broader-suite failure showed `PrismaOrderRepository` helper methods were assuming `extras` arrays were always present, while older repository specs still used minimal order doubles without extras; the repository now tolerates omitted extras defensively.
- 2026-03-30T10:59:32-04:00 [CODE] The order aggregate still had a real immutability leak even without an edit API: `Order.extras` returned the internal array by reference, so callers could mutate extras after order creation unless the aggregate cloned extras on write and read.
- 2026-03-30T11:18:11-04:00 [TOOL] The current codebase was persisting `order_credentials.login`, `summonerName`, and `password` as plaintext in Prisma, and DB integration tests could read the raw values directly before the hardening change.
- 2026-03-30T11:18:11-04:00 [TOOL] `packages/database/prisma.config.ts` had been relying on `envSchema.pick({ DATABASE_URL: true })`; once env parsing gained production refinements, Prisma config loading had to be decoupled from the refined schema to keep DB commands working.
- 2026-03-30T11:18:11-04:00 [TOOL] Importing `fastify` types directly into `http-app.factory.ts` caused a Fastify-version type mismatch with Nest's adapter transitive dependency; the bootstrap hardening helper now uses a local structural type instead.
- 2026-03-30T12:09:00-04:00 [TOOL] The DB-backed orders and payments integration specs remain unsafe to run concurrently because both prepare and mutate the same `elonew_test` database; parallel runs still reproduce false quote-used, missing-record, and FK-cleanup failures.

## [OUTCOMES]
- 2026-03-30T11:22:02-04:00 [TOOL] New backlog item opened as issue `#46`: `[FIX] Normalize workspace and app script conventions`.
- 2026-03-21T08:08:03-04:00 [CODE] Workspace guidance now has an explicit continuity mechanism and a less overlapping issue-start workflow.
- 2026-03-21T08:08:03-04:00 [CODE] `AGENTS.md` is shorter and operational, while historical change notes now live in `.agent/CHANGELOG.md`.
- 2026-03-21T08:23:58-04:00 [TOOL] Verification result: PR `#42` does satisfy issue `#40` as written; it should not be interpreted as completing the full `FR-025` product requirement or issue `#22`.
- 2026-03-21T08:46:08-04:00 [CODE] Issue `#22` non-DB implementation is in place: payment creation now returns a Mercado Pago checkout URL, webhooks reconcile through provider data instead of trusting internal IDs from transport input, and the new gateway reference/status detail fields are wired through the payment aggregate and repository contracts.
- 2026-03-21T12:50Z [TOOL] Issue `#22` now has green targeted verification across unit, integration, e2e, and DB-backed payments lanes after Prisma Client regeneration.
- 2026-03-21T09:16:13-04:00 [TOOL] `nestjs-doctor` is now available as a local Codex skill for future turns after client restart.
- 2026-03-21T13:03Z [CODE] The webhook route now rejects unsupported Mercado Pago topics before provider fetch and ignores replayed deliveries even when the unsigned body event id changes.
- 2026-03-21T13:07Z [CODE] The webhook route now also ignores alias-based replays where the same Mercado Pago notification is delivered once as `payment` and once as `payment.updated`.
- 2026-03-25T13:27:46-04:00 [CODE] The backend now fails fast if an unsupported payment method reaches the Mercado Pago adapter at runtime, and automated coverage now proves intentional support for `credit_card`, `pix`, and `boleto` without adding frontend scope.
- 2026-03-30T09:20:48-04:00 [TOOL] Current stage assessment: backend core checkout/payment/wallet foundations are in place and structurally healthy, but the product is still pre-MVP because the client dashboard/UI layer and several architecture-defined modules are not started.
- 2026-03-30T09:20:48-04:00 [TOOL] Recommended issue sequencing from the current state is `#5` or `#34` first on the backend, then `#33`, then frontend/dashboard work in `#23`, with `#41` only if the team chooses a Brick-based frontend path instead of continuing the current Checkout Pro direction.
- 2026-03-30T10:40:58-04:00 [CODE] Issue `#5` is now materially underway on branch `5-order-extras-deterministic-pricing`: quote creation supports the extras catalog and deterministic modifiers, duplicate extras are rejected at the request boundary, and quote-to-order flows now preserve priced extras in both in-memory and Prisma-backed repository code paths.
- 2026-03-30T10:40:58-04:00 [TOOL] Verification currently green on `pnpm --filter api exec jest --runInBand src/modules/orders/application/use-cases/create-order-quote/create-order-quote.use-case.spec.ts src/modules/orders/application/services/order-coupon.service.spec.ts src/modules/orders/infrastructure/repositories/in-memory-order-checkout.repository.spec.ts`, `pnpm --filter api exec jest --runInBand test/integration/orders/orders.integration.spec.ts`, `pnpm --filter api test:e2e -- orders.e2e-spec.ts`, `pnpm biome:fix:all`, and `pnpm --filter api exec tsc --noEmit -p tsconfig.json`.
- 2026-03-30T10:40:58-04:00 [TOOL] Remaining verification gap for `#5`: DB-backed tests were not rerun because the new schema still needs a human-generated/apply Prisma migration before the database lane matches the updated client and repository code.
- 2026-03-30T10:43:19-04:00 [TOOL] DB-backed verification is currently failing on this branch after migration application; `#5` is not yet complete until the Prisma repository regressions in persisted quote consumption and coupon-linked quote creation are fixed and the DB lanes rerun green.
- 2026-03-30T10:47:39-04:00 [TOOL] Supersedes the previous failing-DB outcome: when run correctly in isolation, both DB-backed lanes are green on this branch, so the current `#5` slice remains verified across unit, integration, e2e, DB integration, Biome, and API typecheck.
- 2026-03-30T10:51:18-04:00 [TOOL] Current verification state is fully green on this branch: `pnpm --filter api test`, `pnpm --filter api test:integration:db -- orders.db.integration.spec.ts`, `pnpm --filter api test:integration:db -- payments.db.integration.spec.ts`, `pnpm biome:fix:all`, and `pnpm --filter api exec tsc --noEmit -p tsconfig.json`.
- 2026-03-30T10:59:32-04:00 [CODE] Issue `#5` now has explicit lock-behavior coverage aligned with the chosen implementation rule: extras are write-once via quote creation and remain unchanged after order creation through later lifecycle transitions.
- 2026-03-30T10:59:32-04:00 [TOOL] Current verification remains green after the final immutability slice: `pnpm --filter api test`, `pnpm --filter api test:integration:db -- orders.db.integration.spec.ts`, `pnpm --filter api test:integration:db -- payments.db.integration.spec.ts`, `pnpm biome:fix:all`, and `pnpm --filter api exec tsc --noEmit -p tsconfig.json`.
- 2026-03-30T11:18:11-04:00 [CODE] The API now encrypts order credentials at rest, enforces production-only secret hardening in env parsing, throttles login/refresh abuse paths, and applies baseline security headers plus an explicit Fastify body limit in the HTTP bootstrap.
- 2026-03-30T11:18:11-04:00 [TOOL] Security hardening verification is green across unit, integration, auth e2e, DB-backed orders/payments lanes, Biome, and API typecheck on branch `5-order-extras-deterministic-pricing`.
- 2026-03-30T12:09:00-04:00 [CODE] The current branch now also closes the immediate longevity and DX review findings: startup-safe encryption-key validation, shared Nest throttling infrastructure, centralized API test settings fixtures, and a cleaner typed HTTP bootstrap seam are in place and verified.
